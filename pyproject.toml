[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "poc-skills"
version = "0.1.0"
description = "Proof-of-concept Fulcra skills for Codex-style agents."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "Yumemishi/Hermemishi/DianaKusunoki" }
]

[tool.setuptools]
packages = ["fulcra_earthquake_annotation"]
